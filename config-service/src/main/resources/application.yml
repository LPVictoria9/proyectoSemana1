server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LPVictoria9/proyectoSemana1.git
          username:  {GET_USER}
          password:  {GET_PWS}
          search-paths: config-data
          default-label: "main"

  security:
    user:
      name: root
      password: s3cr3t
